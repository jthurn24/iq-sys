@model IQI.Intuition.Web.Models.PatientCensus.PatientCensusGrid

@{
    ViewBag.Layout_ContentTitle = "Patient Census Data";
}
<br />
<section class="grid_16 box">
    <h3>@ViewBag.Layout_ContentTitle</h3>
    @Html.Trirand().JQGrid(Model.Grid, Model.GetType().Name)
</section>

@section Scripts
{
    <script type="text/javascript">

        function rowSelect(id) {
            smoothLoad('@Url.Action("Edit","PatientCensus")/?id=' + id);
        }

    </script>
}